# schemas

Shared Pydantic request and response schemas live here.

Phase B keeps endpoint-local schemas close to the health route. Future phases should add reusable conversation, run, tool, and attachment schemas here when they are shared across routers or services.
